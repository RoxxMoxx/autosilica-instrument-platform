# Integration tests

Place cross-crate / end-to-end integration tests here (e.g. spinning
up the Axum router in-process with `tower::ServiceExt::oneshot` and
exercising the HTTP API). Per-crate unit tests live alongside their
source under each `crates/*/src`.
